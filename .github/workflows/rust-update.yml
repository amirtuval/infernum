name: Rust Update Check

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder - real workflow on feature branch"
